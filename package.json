{
  "name": "pimdb",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/pimdb.ts",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
